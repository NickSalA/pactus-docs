get:
  tags:
    - Plantillas
  summary: Listar formatos de plantilla disponibles
  operationId: listTemplateFormats
  parameters:
    - name: document_type
      in: query
      required: false
      schema:
        $ref: '../schemas/schema.yaml#/DocumentType'
  responses:
    '200':
      description: Lista de formatos obtenida correctamente.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/schema.yaml#/TemplateFormatResponse'
